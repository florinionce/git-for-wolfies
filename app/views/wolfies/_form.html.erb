<%= form_for(wolfy) do |f| %>
  <% if wolfy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wolfy.errors.count, "error") %> prohibited this wolfy from being saved:</h2>

      <ul>
      <% wolfy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :wolfie_level %>
    <%= f.number_field :wolfie_level %>
  </div>

  <div class="field">
    <%= f.label :dob %>
    <%= f.datetime_select :dob %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
